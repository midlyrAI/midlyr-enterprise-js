# `@midlyr/mcp`

Scaffold for the public MidLyr MCP package.

This package depends on `@midlyr/sdk` through its public package exports. It currently exposes only scaffold markers so the workspace can build and verify package boundaries. Real MCP behavior is deferred to later work.
