# `@midlyr/sdk`

Scaffold for the public MidLyr JavaScript SDK package.

This package currently exposes only a scaffold marker so the workspace can build and verify package boundaries. Real SDK API methods are deferred to later work.
